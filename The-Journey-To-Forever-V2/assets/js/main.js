/* ==========================================================
   MAIN
========================================================== */

window.addEventListener("load", () => {

    gsap.set("#envelope", {
        autoAlpha: 0
    });

    gsap.set("#invitation", {
        autoAlpha: 0,
        y: 80
    });

    gsap.set("#story", {
        autoAlpha: 0,
        y: 80
    });

    gsap.set("#gallery", {
        autoAlpha: 0,
        y: 80
    });

    gsap.set("#countdown", {
        autoAlpha: 0,
        y: 80
    });

    gsap.set("#rsvp", {
        autoAlpha: 0,
        y: 80
    });

});